interface:
  display_name: "Maintainer"
  short_description: "Audit and maintain repository skills and plugins."
  brand_color: "#B85C00"
  default_prompt: "Use $maintainer to inspect the requested packages, apply authorized improvements, and verify the result."
policy:
  allow_implicit_invocation: false
