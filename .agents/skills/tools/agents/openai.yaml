interface:
  display_name: "Tools"
  short_description: "Orchestrate targeted skill upgrades, metadata/doc sync, consistency checks, and upstream benchmark workflows for skills in this repository."
  brand_color: "#B85C00"
  default_prompt: "Run the requested skill maintenance workflow for this repository, including targeted upgrades, metadata/doc sync, audits, refreshes, or benchmarks, route to the correct playbook, and report clear pass/fail findings with next actions."
