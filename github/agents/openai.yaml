interface:
  display_name: "GitHub"
  short_description: "Use GitHub CLI (gh) for repository-scoped issues, pull requests, Actions runs/logs, releases, and tags. Default to the current git project unless another `owner/repo` is provided."
  brand_color: "#1F9D55"
  icon_small: "./assets/github-small.svg"
  icon_large: "./assets/github.png"
  default_prompt: "Use GitHub CLI (gh) to complete the requested repository, issue, pull request, Actions run/log, release, or tag task."
