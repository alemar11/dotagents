interface:
  display_name: "Study"
  short_description: "Grill a handoff, then study with up to five workers."
  default_prompt: "Use $study to refine my handoff through $se:grilling, then run read-only analysis in the exact saved local project."
  brand_color: "#0E7490"
policy:
  allow_implicit_invocation: false
