interface:
  display_name: "GitHub Triage"
  short_description: "Inspect current-repo GitHub queues and guard issue closure."
  brand_color: "#0369A1"
  default_prompt: "Use $github-triage to produce a URL-first current-repo issue and PR triage report with direct gh commands and no mutations unless asked. Before closing partial work, require a linked or proposed follow-up issue for deferred acceptance criteria."
