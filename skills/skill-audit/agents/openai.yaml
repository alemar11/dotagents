interface:
  display_name: "Skill Audit"
  short_description: "Explicit-only read-only audit of Codex skills and plugins."
  brand_color: "#C06C84"
  default_prompt: "Use $skill-audit only when explicitly requested to audit named Codex skills or plugins read-only; return evidence-backed findings."

policy:
  allow_implicit_invocation: false
