interface:
  display_name: "Crusty"
  short_description: "Advisory-only critique for decisions and implementations."
  brand_color: "#9A6B2F"
  default_prompt: "Use $crusty as an advisory-only skeptic to challenge this decision or evaluate this implementation from local or supplied evidence, report meaningful defects and test gaps, and recommend fixes without editing project files."
policy:
  allow_implicit_invocation: false
