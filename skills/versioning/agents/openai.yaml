interface:
  display_name: "Versioning"
  short_description: "Plan and apply SemVer tags with guarded Actions"
  brand_color: "#047857"
  default_prompt: "Use $versioning to resolve the requested version, tag, migration, or release workflow within its authorized scope."
