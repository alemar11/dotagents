interface:
  display_name: "G"
  short_description: "Manage Git and GitHub workflows safely"
  brand_color: "#24292F"
  default_prompt: "Use $g to handle this Git or GitHub workflow safely."

policy:
  allow_implicit_invocation: true
