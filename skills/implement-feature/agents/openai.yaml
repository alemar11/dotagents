interface:
  display_name: "Implement Feature"
  short_description: "Implement a ready Feature Spec frontier in App tasks."
  brand_color: "#6A7F3F"
  default_prompt: "Use $implement-feature to execute the dependency-ready Feature Spec frontier in visible App worktree tasks through implementation, validation, current-head review, configured CI, and merge-ready PR closeout."
policy:
  allow_implicit_invocation: false
