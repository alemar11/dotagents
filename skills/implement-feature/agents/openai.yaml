interface:
  display_name: "Implement Feature Spec"
  short_description: "Coordinate end-to-end Feature Spec workers in visible Codex tasks."
  brand_color: "#6A7F3F"
  default_prompt: "Use $implement-feature to coordinate visible Codex worker tasks and ChatGPT-created worktrees that execute the dependency-ready Feature Spec frontier end to end through implementation, validation, current-head review, tracker proof, and the declared GitHub PR or local-branch closeout."
policy:
  allow_implicit_invocation: false
