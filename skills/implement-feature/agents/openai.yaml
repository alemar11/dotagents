interface:
  display_name: "Implement Feature"
  short_description: "Implement ready Feature Specs as merge-ready App PRs."
  brand_color: "#6A7F3F"
  default_prompt: "Use $implement-feature to execute a ready Feature Spec bundle in visible App tasks through incremental AutoReview, current-head review, configured CI, and pull-request-ready-for-merge-but-not-merged closeout."
policy:
  allow_implicit_invocation: false
