interface:
  display_name: "Implement Feature"
  short_description: "Coordinate end-to-end Feature Spec workers in App tasks."
  brand_color: "#6A7F3F"
  default_prompt: "Use $implement-feature to coordinate visible App worktree workers that execute the dependency-ready Feature Spec frontier end to end through implementation, validation, current-head review, configured CI, tracker proof, and merge-ready PR closeout."
policy:
  allow_implicit_invocation: false
