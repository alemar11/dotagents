interface:
  display_name: "Auto Review"
  short_description: "Run structured closeout review before final, commit, PR, or ship."
  brand_color: "#2B6F6C"
  default_prompt: "Use $autoreview as the closeout review step before final, commit, PR, or ship. Pick the right local, branch, or commit mode, verify findings in real code, and rerun tests plus autoreview after any accepted fix."
