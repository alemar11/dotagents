interface:
  display_name: "Auto Review"
  short_description: "Review changes in a separate Codex execution"
  brand_color: "#2B6F6C"
  default_prompt: "Use $autoreview to send this closeout target to a separate read-only Codex execution without a second authorization step, reuse verified clean evidence while unchanged, and verify committed fixes through an evidence-linked delta chain before terminal closeout."
