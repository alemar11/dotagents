interface:
  display_name: "Codex CLI"
  short_description: "Launch a complete prompt in a separate Codex CLI task"
  default_prompt: "Use $codex-cli for one explicit delegated task: preserve the complete prompt, default to gpt-5.6-sol at medium, honor any explicit model or reasoning override, and choose both only when the caller grants carte blanche."
  brand_color: "#14B8A6"
policy:
  allow_implicit_invocation: false
