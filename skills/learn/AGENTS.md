# Learn Maintenance

The entrypoint registry owns graph edges; `references/states.md` owns state
meanings. Keep every node reachable and give terminal nodes no outgoing edges.
Options and durable content are not workflow checkpoints. Preserve the
separation between read-only inspection, explicit capture, and repository
bootstrap.
