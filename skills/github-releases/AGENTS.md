# GitHub Releases Maintenance

This skill owns release, tag, note, asset, and package lifecycle through direct
`gh`. Do not move release policy into a shared multi-domain helper. Invocation
fields live in `SKILL.md`. Keep SemVer selection in `$versioning`.
