interface:
  display_name: "Boost"
  short_description: "Plan or research read-only with up to five workers."
  default_prompt: "Use $boost to plan, research, or analyze in an exact saved local project with an absolute five-worker cap, no nested Boost runs or project edits, archival requests for terminal workers, and one visible Markdown-summary orchestrator."
  brand_color: "#0E7490"
policy:
  allow_implicit_invocation: false
