interface:
  display_name: "Git Commit"
  short_description: "Create precise git commits from local changes."
  brand_color: "#64748B"
  default_prompt: "Use $git-commit to inspect local changes, stage explicit paths, write a clear commit message, commit, optionally push without opening a PR, and use issue-closing trailers only for fully satisfied issues."
