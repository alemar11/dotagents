interface:
  display_name: "Plan Feature Spec"
  short_description: "Converge complete applied or proposed planning bundles."
  brand_color: "#10B981"
  default_prompt: "Use $plan-feature to converge feature intent or a durable Feature Spec into one complete planning bundle, applying it with agent-ready issue metadata or returning a non-executable proposal grounded in project context."
policy:
  allow_implicit_invocation: false
