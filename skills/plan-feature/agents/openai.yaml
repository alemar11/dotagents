interface:
  display_name: "Plan Feature"
  short_description: "Plan a feature through PRD and agent-ready issues, or run prd-only and issues-from-existing-prd modes."
  brand_color: "#10B981"
  default_prompt: "Use $plan-feature to choose the planning mode, ensure project-memory setup, write or publish the PRD to the configured tracker by default, and split it into hardened vertical issues when requested."
policy:
  allow_implicit_invocation: false
