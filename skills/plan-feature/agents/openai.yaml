interface:
  display_name: "Plan Feature Spec"
  short_description: "Converge complete planning bundles for publication or preview."
  brand_color: "#10B981"
  default_prompt: "Use $plan-feature to converge feature intent or a durable Feature Spec into one complete planning bundle, publishing it with agent-ready issue metadata or returning a non-executable preview grounded in project context."
policy:
  allow_implicit_invocation: false
