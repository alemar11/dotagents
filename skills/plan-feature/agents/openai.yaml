interface:
  display_name: "Plan Feature"
  short_description: "Plan a feature through PRD and agent-ready issues, or run prd-only and issues-from-existing-prd modes."
  brand_color: "#10B981"
  default_prompt: "Use $plan-feature to choose the planning mode, ensure project-memory setup, produce or use a PRD, split it into hardened vertical issues when requested, and keep local writes separate from hosted tracker mutation."
policy:
  allow_implicit_invocation: false
