interface:
  display_name: "Setup Project Memory"
  short_description: "Configure project memory for repos, monorepos, or orchestrator workspaces."
  brand_color: "#3B5BDB"
  default_prompt: "Use $setup-project-memory to inspect this workspace, choose repo, monorepo, or orchestrator setup mode, confirm issue tracker, triage types and labels, and domain-memory layout, then write AGENTS.md plus project-memory/agents files and seed CONTEXT.md or ADRs when existing evidence is strong."
