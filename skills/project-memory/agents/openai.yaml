interface:
  display_name: "Project Memory"
  short_description: "Configure tracker, delivery, context, and localization memory."
  brand_color: "#3B5BDB"
  default_prompt: "Use $project-memory to configure only the requested tracker, delivery, domain, localization, or AGENTS.md-pointer slice. Treat an explicit setup or configure request as write authority, infer safe defaults from repo evidence, and ask only for materially ambiguous target or behavior values."
