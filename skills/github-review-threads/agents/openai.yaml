interface:
  display_name: "GitHub Review Threads"
  short_description: "Inspect and mutate pull request review threads"
  brand_color: "#9333EA"
  default_prompt: "Use $github-review-threads to inspect pull request review threads and perform authorized request, wait, reply, or resolve provider operations."
