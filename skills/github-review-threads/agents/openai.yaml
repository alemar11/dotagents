interface:
  display_name: "GitHub Review Threads"
  short_description: "Inspect PR review threads, draft or post replies, and manage comments."
  brand_color: "#9333EA"
  default_prompt: "Use $github-review-threads to inspect PR review threads, draft comments or replies by default, and post only with explicit user or calling-skill authority through scripts/reviews first; use direct gh only as a stated fallback."
