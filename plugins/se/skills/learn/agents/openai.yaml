interface:
  display_name: "🗂️ Learn Project Context"
  short_description: "Maintain durable project knowledge and decisions."
  brand_color: "#C2410C"
  default_prompt: "Use $se:learn to capture or update durable repository knowledge, decisions, localization guidance, or code review rules. Change only the requested local knowledge surfaces and confirm uncertain facts before preserving them."
policy:
  allow_implicit_invocation: false
