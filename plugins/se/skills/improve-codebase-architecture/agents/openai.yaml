interface:
  display_name: "Improve Codebase Architecture"
  short_description: "Find and pressure-test architecture improvements."
  brand_color: "#4338CA"
  default_prompt: "Use $se:improve-codebase-architecture to inspect this repo for evidence-backed architecture improvements, then pressure-test my selection one decision at a time and prepare accepted durable decisions for $se:learn; write them only with explicit scoped capture authority."
