interface:
  display_name: "GitHub Reviews"
  short_description: "Prefer `gh` for straightforward review submission and comments, and use shared `ghflow` helpers for review-thread triage and reply routing."
  brand_color: "#DC2626"
  icon_small: "./assets/github-small.svg"
  icon_large: "./assets/github.png"
  default_prompt: "Use the bundled GitHub Reviews skill to prefer direct `gh` review commands for simple cases, and use shared `ghflow` helpers when the job needs review-thread triage, reply routing, or higher-level comment handling."
