interface:
  display_name: "GitHub Reviews"
  short_description: "Use when handling PR reviews, review threads, replies, or review submission; resolve `ghflow` only for thread routing."
  brand_color: "#DC2626"
  icon_small: "./assets/github-small.svg"
  icon_large: "./assets/github.png"
  default_prompt: "Use the bundled GitHub Reviews skill to prefer direct `gh` review commands for simple cases, and resolve the installed `ghflow` artifact when the job needs review-thread triage, reply routing, or higher-level comment handling."
