interface:
  display_name: "GitHub Triage"
  short_description: "Use when orienting around repos, issues, PR metadata, or authenticated-user stars/lists; prefer direct `gh` for repo work."
  brand_color: "#0F766E"
  icon_small: "./assets/github-small.svg"
  icon_large: "./assets/github.png"
  default_prompt: "Use the bundled GitHub Triage skill to prefer direct `gh` commands for repository, issue, and PR work, use raw `gh issue ...` sequences for cross-repo issue copy or move and close-with-evidence flows, and resolve the installed `ghflow` artifact only for authenticated-user stars and star lists."
