interface:
  display_name: "Git Commit"
  short_description: "Create or push explicit Git commits"
  brand_color: "#64748B"
  default_prompt: "Use $gitstack:git-commit to inspect the intended scope, stage explicit paths, create the requested regular or targeted commit, and push only when requested; never infer fixup from feedback alone and never autosquash."
