interface:
  display_name: "Yeet"
  short_description: "Use when local work should become a pushed branch and draft PR through `git`, bundled `git-commit`, and resolved `ghflow` publish helpers."
  brand_color: "#F97316"
  icon_small: "./assets/github-small.svg"
  icon_large: "./assets/github.png"
  default_prompt: "Use the bundled Yeet skill to inspect a local worktree with `git`, hand commit creation to bundled `git-commit`, and resolve the installed `ghflow` artifact before using publish helpers for repo-aware publish-context inspection and PR open-or-reuse behavior."
