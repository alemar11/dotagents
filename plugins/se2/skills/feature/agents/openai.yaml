interface:
  display_name: "📚 Feature Planning"
  short_description: "Consolidate a feature and derive ready tasks."
  brand_color: "#BE185D"
  default_prompt: "Use $se2:feature for an explicit repository-scoped Feature graph run; consolidate caller-proposed splits into the smallest independently deliverable Feature boundary, derive stable acceptance criteria and vertical Task dependencies, support maintenance through the same graph, and return the complete bundle without implementing code."
policy:
  allow_implicit_invocation: false
