interface:
  display_name: "Feature Graph"
  short_description: "Traverse a Markdown-defined Feature workflow graph."
  brand_color: "#BE185D"
  default_prompt: "Use $se2:feature to load repository AGENTS.md context, converge repository-owned Feature issues and vertical Task dependency graphs, support Feature maintenance through the same graph, follow explicit node transitions, and return the complete bundle without implementing code."
policy:
  allow_implicit_invocation: false
