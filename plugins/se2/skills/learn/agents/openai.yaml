interface:
  display_name: "🗂️ Learn Project Context"
  short_description: "Capture and maintain durable knowledge about a repository."
  brand_color: "#C2410C"
  default_prompt: "Use $se2:learn as an explicit graph-first repository-context workflow; resolve the smallest memory_slice, route conditional detail through the selected context surfaces, require confirmation for durable capture, and preserve the exact Code Review Rules section."
policy:
  allow_implicit_invocation: false
