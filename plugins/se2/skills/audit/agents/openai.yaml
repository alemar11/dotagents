interface:
  display_name: "🔎 Audit"
  short_description: "Review active SE2 sessions and report workflow issues read-only."
  brand_color: "#166534"
  default_prompt: "Use $se2:audit explicitly to monitor active SE2 skill sessions and report evidence-backed graph conformance, feedback, bugs, regressions, and improvements without mutations."
policy:
  allow_implicit_invocation: false
