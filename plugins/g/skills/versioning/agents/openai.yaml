interface:
  display_name: "Versioning"
  short_description: "Plan SemVer tags, migrations, and guarded release Actions"
  brand_color: "#0F766E"
  default_prompt: "Use $g:versioning to validate the shared vX.Y.Z convention, calculate context-aware release suggestions, bootstrap tag-less repositories, plan stable legacy-tag aliases, or install the guarded GitHub Actions release topology and versioned resolver asset; never read or edit application code for Action authoring, never apply a noncanonical tag even when requested or confirmed, and always request separate explicit confirmation before applying a canonical tag."
